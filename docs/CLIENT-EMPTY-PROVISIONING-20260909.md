# Cadastro administrativo de loja — 9 de setembro de 2026

Organização `org-93e60813-215c-4fc0-b4e2-4897e67fc9f1`, slug de apresentação `scalon-modas`; plano exclusivo `denize-batista-franco`. Dados fiscais e contato informados pelo usuário foram armazenados somente na matriz do banco tenant. O nome foi preservado literalmente, sem corrigir o prefixo numérico para fazê-lo coincidir com o CNPJ.

Plano baseado em `fashion-demo`, preservando acessos efetivos legados e removendo `service-orders`, `purchases`, `production`, `accounts`, `reconciliation`. Preços de referência 549/mês e 5490/ano, 25 usuários; não constituem cobrança emitida. Organização e plano guardam as mesmas permissões explícitas. Conta Billing usa o ID interno e está pendente, sem job de criação remota. Login inicial do responsável não foi criado.

O provisionador geral tenta importar demonstração de autopeças, inadequada para um cliente real. Foi utilizado um executor administrativo específico, root-owned 0700, restrito a esta organização, sem seed: `/usr/local/libexec/nalven/provision-denize-empty.sh` e `/usr/local/libexec/nalven/run-denize-migration-bundle.sh`. Não se alterou o provisionador geral nem se concedeu sudo permanente novo. Fontes revisadas e SQL inicial estão no diretório privado `/tmp/nalven-client-create.NgiuRV`.

O executor usa o bundle imutável de migrations existente, verifica seu manifesto e executa como `nalven-migrator`. A credencial é passada por stdin, não por argumento de processo. O banco `nalven_t_scalon_modas` recebeu 120 migrations concluídas, nenhuma carga demonstrativa, uma matriz com dados reais e roles runtime/migrator/autoridades segregadas. Não foi presumido regime tributário, CNAE, certificado ou configuração de emissão fiscal.

Na primeira tentativa, a etapa de grants acessou `_prisma_migrations` antes da primeira migration e falhou; não houve carga parcial de dados comerciais. A segunda tentativa identificou CONNECT ausente após a preparação inicial. Para este banco novo, CONNECT foi concedido explicitamente às suas 11 roles dedicadas; migrations vieram antes da reconciliação completa de grants. A execução seguinte terminou com sucesso. Permissões finais verificadas: runtime pode ler matriz, não pode criar objetos no schema nem alterar o histórico Prisma. Job de banco concluído e criação auditada.

Atualização do acesso: administrador da loja criado com o e-mail informado, membership `owner`, perfil proprietário e acesso à matriz. Papel global `user`, sem privilégios de superadmin. Ativação ocorreu após a preparação do perfil tenant, com auditoria nos dois bancos. Foi disponibilizado link de definição de senha de uso único, com validade de 30 minutos; apenas o hash do token é armazenado, sem SMTP. Não foi realizado login com senha escolhida pelo responsável.

Certificado A1 importado na matriz (filial 1, certificado 1), ativo, após validação da senha fornecida. CNPJ do certificado confirmado como `62119228000152`; validade de 27/08/2026 19:48:08 UTC até 27/08/2027 19:48:08 UTC. Integridade PKCS#12, correspondência da chave privada, assinatura/verificação de desafio local e leitura de conteúdo/senha criptografados foram verificadas. Armazenamento AES-256-GCM compatível com `lib/secrets.ts`, transação serializável e evento `fiscal_certificate.saved`. Não houve emissão, consulta SEFAZ ou validação online de revogação/cadeia; o teste local não comprova prontidão de emissão fiscal. Senha e chave privada não foram registradas neste documento.

Pendências: definição da senha pelo responsável e regularização/configuração da licença e assinatura Billing. O provisionador geral ainda precisa de uma correção permanente e homologada para bancos vazios, sem demonstração; estes executores específicos não representam essa correção global. Nenhum cliente, licença ou cobrança foi criado no sistema externo.
