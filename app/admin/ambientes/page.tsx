import {controlDb} from '@/db/control';
export default async function Ambientes(){const rows=await controlDb.tenantDatabase.findMany({include:{organization:{select:{name:true,status:true}}},orderBy:{createdAt:'desc'}});return <><div className="module-heading"><div><p>INFRAESTRUTURA TÉCNICA</p><h1>Ambientes dos tenants</h1><span>Bancos PostgreSQL isolados usados pelo ERP. Não representam contas bancárias ou cobranças.</span></div></div><section className="control-panel"><div className="table-tools"><strong>Isolamento de dados</strong><span>{rows.length} ambientes</span></div><div className="admin-table-wrap"><table><thead><tr><th>Organização</th><th>Banco técnico</th><th>Chave de configuração</th><th>Schema</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.organization.name}</td><td>{x.databaseName}</td><td>{x.configKey}</td><td>{x.schemaVersion}</td><td>{x.status}</td></tr>)}</tbody></table></div></section></>}
