import { controlDb } from "@/db/control"; import Link from "next/link";
export default async function Dashboard(){const [orgs,users,databases,tickets,jobs,backups,posts,media]=await Promise.all([controlDb.organization.count(),controlDb.user.count(),controlDb.tenantDatabase.count({where:{status:'active'}}),controlDb.ticket.count({where:{status:{not:'resolved'}}}),controlDb.provisioningJob.count({where:{status:'pending'}}),controlDb.backupRun.findFirst({orderBy:{startedAt:'desc'}}),controlDb.blogPost.count(),controlDb.mediaAsset.count()]);return <><div className="module-heading"><div><p>VISÃO CONSOLIDADA</p><h1>Visão geral</h1><span>Acompanhe clientes, infraestrutura, conteúdo e operações.</span></div><div className="page-actions"><Link href="/admin/gestao">Editar site</Link><Link className="primary" href="/admin/organizacoes">Organizações</Link></div></div><section className="metric-grid">{[["Organizações",orgs],["Usuários",users],["Bancos ativos",databases],["Tickets abertos",tickets],["Fila pendente",jobs],["Conteúdos",posts+media]].map(([label,value])=><article key={label as string}><small>{label}</small><strong>{value}</strong></article>)}</section><div className="dashboard-columns"><section className="control-panel"><header><div><h2>Acesso rápido</h2><p>Principais áreas de administração</p></div></header><div className="quick-grid">{[["Organizações","Clientes, planos e acessos","/admin/organizacoes"],["Biblioteca","Imagens e documentos","/admin/biblioteca"],["SEO","Metadados e indexação","/admin/seo"],["Sistema","Saúde e infraestrutura","/admin/sistema"]].map(([t,d,h])=><Link href={h} key={h}><strong>{t}</strong><span>{d}</span></Link>)}</div></section><section className="control-panel"><header><div><h2>Estado operacional</h2><p>Últimos processos do ambiente</p></div></header><div className="health-list"><p><i className="ok"/> Aplicação em execução</p><p><i className="ok"/> {databases} bancos isolados ativos</p><p><i className={jobs?'warn':'ok'}/> {jobs} provisionamentos pendentes</p><p><i className={backups?.status==='completed'?'ok':'warn'}/> Último backup: {backups?.status||'sem registro'}</p></div></section></div></>}
